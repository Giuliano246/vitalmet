# CLAUDE.md — VitalStock ERP

## Proyecto
VitalStock es un ERP single-file (HTML + Supabase) para Vitalmet SA, empresa metalúrgica argentina que fabrica productos para la industria petrolera (uniones, válvulas, anillos BX, codos, campanas, etc.).

**Owner:** Giuliano
**Empresa:** Vitalmet SA — Perú 246, Villa Martelli, Buenos Aires
**Web:** vitalmetsa.com
**ERP URL:** https://erp.vitalmetsa.com (CNAME → Netlify, site `vitalmetstock`)

## Stack
- **Frontend:** HTML único (`index.html`, ~8.000 líneas), vanilla JS, CSS embebido. Libs por CDN (cdnjs): jsPDF + autotable, SheetJS (xlsx), Chart.js.
- **Backend/DB:** Supabase (auth + PostgreSQL 17, REST vía PostgREST)
- **Hosting:** Netlify (deploy automático al pushear a `main` de github.com/Giuliano246/vitalmet)
- **Headers:** `netlify.toml` define CSP + security headers. **Si se agrega un CDN, API externa o fuente nueva, agregar el origen al CSP o se bloquea en producción.**
- **Facturación AFIP:** Edge Function `supabase/functions/facturacion` (deploy: `npx supabase functions deploy facturacion --no-verify-jwt`). WSAA firma CMS con node-forge; TA cacheado en tabla `afip_ta` (migración 063). Cert X.509 prod vigente hasta 2028-08-03, PV 4, ambiente `produccion`. Secrets: AFIP_CUIT/AFIP_ENVIRONMENT/AFIP_CERT_B64/AFIP_KEY_B64/BILLING_API_KEY. El microservicio de Railway (`~/vitalmet-billing`) quedó obsoleto 2026-08-04.

## Supabase
- **URL:** `https://dqvlqhaxgvtilhiuatpv.supabase.co` (anon key en el HTML, ~línea 1843)
- **RLS:** activado en TODAS las tablas. Patrón: policy `tenant_isolation FOR ALL TO authenticated USING (empresa_id = public.current_empresa_id())` + `WITH CHECK` igual. `current_empresa_id()` lee el JWT.
- **empresa_id del usuario:** `a0a19507-2a50-4e80-a716-e9459f51d653` — hardcodear directo en migrations y scripts (single-tenant en la práctica).
- **Migraciones:** `migrations/001` a `067`. Convención: BEGIN/COMMIT, idempotentes (IF NOT EXISTS / DROP IF EXISTS), retrocompatibles, query de verificación comentada al pie. **El SQL se corre en el SQL Editor ANTES de deployar el frontend.** Para dry-run contra prod: Management API con COMMIT→ROLLBACK.
- **Integridad (migración 023):** RPCs atómicas `guardar_venta` / `anular_venta` / `convertir_presupuesto` / `crear_asiento` / `consumir_barra` / `upsert_cliente` (SECURITY INVOKER), trigger de partida doble diferido, numeración de asientos por DB, guard anti-edición de ventas con CAE, `audit_log` + trigger `trg_audit` (fn_audit) en tablas con plata.
- **Integridad (migración 055):** trigger + UNIQUE anti-duplicado de `facturas_recibidas` (empresa, proveedor, tipo, letra, nro); `recepciones_oc.oc_item_id` es ON DELETE **RESTRICT** (editar OC con recepciones ya no reescribe ítems — el frontend solo actualiza cabecera); RPC `eliminar_op` (repone kg a la barra, bloquea si hay PT/registros de calidad/NC/tratamientos/signoffs); numeración de presupuestos server-side (`trg_presupuesto_nro`, P-nnnn con advisory lock); RPCs `guardar_permisos` y `ajustar_precios_pct` atómicas; `clientes.condicion_fiscal` + `config_contable.punto_venta`. La facturación (`emitirFactura`) deriva letra A/B de la condición fiscal del cliente (RG 5003: RI y monotributo→A, exento y CF→B), convierte USD→ARS al TC vendedor BNA del día y usa el punto de venta configurado — funciones puras `tipoComprobanteVentaAFIP` / `buildItemsFactura` con tests en `tests/facturacion.test.js`. Tarifa operario unificada en `tarifaOperarioUSD()` (lee `empresas.tarifa_operario_usd_h`).
- **Ciclo del dinero (migración 056):** tabla `retenciones_sufridas` (certificados Gcias/IIBB/SUSS/IVA que aplican los clientes al cobrar; NO está en loadAll — se consulta on-demand para el exportable) + config `cta_ret_*` (defaults del plan real: 113018/113006/113019/113005) y `cta_cheques_cartera`/`cta_cheques_emitidos` (111006/212003). Cobros con retenciones: `armarLineasCobro` (pura, tests en `tests/ciclo-dinero.test.js`) arma banco + retenciones al debe / Deudores por el total; la cta cte suma todos los debe. Cheques: `contabilizarCheque` genera asientos por estado (depositado/rechazado/debitado; origen_tipo `cheque-*` idempotente); el alta contable la hace Cobros y Pagos método "cheque"; rechazo recibido = asiento tipo `auto-cheque-rechazo` que computeCtaCte suma como cargo (deuda revive). Costo real de OP: al completar (cambiarEstado o saveEditOP) `persistirCostoRealOP` congela `ordenes_produccion.costo_real_usd` (por unidad, vía `computeCostoRealOP` pura) y valúa el PT del lote a ese costo. Exportables nuevos: IVA ventas con letra/signo (`cbteAfipInfo`/`alicuotaImplicita` puras), Posición IVA del mes, Retenciones sufridas.
- **Compras fiscal (migración 054):** `registrar_factura_recibida(p_factura, p_impuestos, p_override, p_motivo)` hace 3-way match + asiento + tax lines en `factura_recibida_impuestos` (una fila por alícuota de IVA / percepción, patrón SAP). Regla: montos SIEMPRE positivos en DB, el signo lo da `facturas_recibidas.tipo` (`nota_credito` resta). El RPC valida la aritmética (total = bases + no gravado + exento + IVA + percepciones ±$0.01) y que letra B/C/X no discrimine IVA. Facturas sin OC y NC/ND requieren `cta_imputacion_id`. Espejo cliente: `computeTotalesFactura` / `validarDesgloseFactura` (tests en `tests/compras-fiscal.test.js`).
- **Automatización (migración 057):** worker de mails server-side — cron pg_cron `vitalmet-mailer` (cada 15', 11-22 UTC) invoca la Edge Function `supabase/functions/vitalmet-mailer` (deploy con `npx supabase functions deploy vitalmet-mailer`; secret `MAILER_SECRET` = `push_config.mailer_secret`), que despacha `email_queue` estado `aprobado` por Microsoft Graph con las credenciales de `integracion_microsoft` y copia a `email_log`; sin integración activa es no-op (los aprobados esperan). `clientes.limite_credito` (USD, NULL = sin límite) + semáforo `chequearCredito`/`avisosCreditoCliente` (confirm en saveVenta/convertirPresupuesto — avisa mora vencida y límite excedido, no bloquea). Reposición: `calcReordenMaterial` descuenta OC en tránsito (`enTransitoMaterial`) del "A pedir" y `generarOCDesdeReposicion` pre-carga una OC con la última compra del material (`ultimaCompraMaterial`). `clientesInactivos` (90/180 días) alimenta computeAlertas y el mail de reactivación; avisos de estado de pedido (`encolarEmailEstadoVenta`: confirmado al convertir presupuesto, despachado al pasar a entregado) van a la cola con aprobación. Puras con tests en `tests/automatizacion.test.js`.
- **Inventario auditable (migración 058):** kardex `movimientos_stock` — triggers `trg_kardex` (fn_kardex SECURITY DEFINER) en barras/productos_terminados/insumos registran TODO delta de cantidad con saldo, usuario y etiqueta; la tabla es solo-SELECT vía API (sin batería contador/audit: es en sí un log). Etiquetas via `set_config('app.stock_ctx','tipo:ref',true)` transaccional — las RPCs de stock (consumir_barra, guardar_venta, anular_venta, fn_venta_entrega_stock, eliminar_op) están RE-EMITIDAS en 058 con esa línea (si se editan, editar la versión de 058, no la vieja). RPC `ajustar_stock(tipo,item,cantidad,motivo[,origen])`: motivo obligatorio + asiento stock↔AJUSTES DE INVENTARIO (cuentas config `cta_stock_mp/pt/insumos` = 114002/114001/114003, `cta_ajuste_inventario` = 421099 creada ahí). Frontend: `ajustarStockSiCambio` intercepta cambios de cantidad en saveEditBarra/savePT/saveInsumo (prompt de motivo → RPC). Conteo físico: `conteos_stock`+`conteo_items` (batería RLS completa, nro CT-nnnn) + RPC `cerrar_conteo` (aplica diferencias contra el stock ACTUAL vía ajustar_stock). Recepción de OC valúa la barra (`costo_usd_unidad` desde el ítem de OC, ARS→USD por tipo_cambio; fix: unidad `mts` ahora carga kg_disponibles). Valuación: `computeValuacionMP`/`computeValuacionPT` puras (tests/inventario.test.js) + exportable "Valuación de inventario" + card en MP.
- **Calidad certificable (migración 059):** tablas `certificados_emitidos` (CC-nnnn por venta, UNIQUE venta_id, registrarCertEmitido en generateCertConformidadPDF — ahora async), `revisiones_direccion` (RD-nnnn, kpis jsonb congelado por `computeKpisCalidad` pura en tests/sgc.test.js; cerrada = inmutable por trigger `fn_revdir_inmutable`), `auditorias_internas` + `auditoria_hallazgos` (AI-nnnn; hallazgo NC → POST no_conformidades origen 'auditoria' + link nc_id), `competencias` (persona texto libre × habilidad, vence → computeAlertas). Todas con batería RLS completa (helper pg_temp en la migración). **Modo auditor**: `usuarios.es_auditor` + `es_auditor()`; `es_contador()` REDEFINIDA como (es_contador OR es_auditor) → todas las policies contador_no_* y fn_contador_guard cubren al auditor sin tablas nuevas; `guardar_permisos` re-emitida con `p_es_auditor` (6 params — la de 5 params fue DROPeada). Navegación: grupo calidad += tab 'sgc' con sub-tabs kpiscalidad/revdir/auditorias/competencias; módulo de permisos nuevo `sgc`.
- **Integridad fiscal (migración 067, auditoría contable 2026-08-06):** (1) INMUTABILIDAD: triggers `trg_asiento_inmutable` (asientos: DELETE solo borradores + bloqueado en período cerrado — la 032 solo cubría INSERT/UPDATE; UPDATE por REST de un confirmado/anulado solo permite el flip de estado con contenido intacto) y `trg_lineas_inmutables` (asiento_lineas: INSERT/UPDATE/DELETE por REST solo si el asiento está en borrador). La RPC `crear_asiento` fue RE-EMITIDA (base 024 — ediciones futuras parten de 067) con `set_config('app.asiento_rpc','1',true)` transaction-local: los triggers dejan pasar a la RPC (flujos idempotentes de facturas/cheques siguen funcionando); TODO flujo que toque asientos/líneas debe ir por la RPC. UI: botón eliminar solo en borradores, estadoSelect confirmado→[confirmado,anulado]. (2) TRAZABILIDAD ARCA: tabla `afip_eventos` (INSERT solo service_role, SELECT solo admin) — la Edge Function `facturacion` loguea TODA solicitud de CAE (aprobada/rechazada/error) con SOAP crudo; `emitirFactura` manda `venta_id`+`empresa_id` en el payload. (3) NC DE VENTAS: `facturas_emitidas.factura_asociada_id` + Edge Function con bloque `CbtesAsoc` (RG 4540; obligatorio para tipos NC/ND, Cuit = emisor propio) → botón "NC" en ventas facturadas abre `modal-nota-credito` (total o parcial por monto ARS IVA incluido; controla lo ya acreditado por NC previas), `emitirNotaCredito` pide CAE tipo 3/8 (misma letra que la factura, `NC_TIPO_BY_FACTURA`), `generarAsientoNC` hace el asiento inverso (debe Ventas+IVA Débito / haber Deudores, tipo 'auto-nc', origen 'nota-credito', con cliente_id y TC del día) y computeCtaCte lo resta como pago. Desglose con redondeo espejo de la EF: `computeNetoIvaDesdeTotal` pura (tests en `tests/nota-credito.test.js`). Deploy EF: `npx supabase functions deploy facturacion --no-verify-jwt`.
- **Sueldos Fase 2 (migración 066):** `empleados.valor_hora` + parte de horas desde los timers de Modo Planta — los timers usan el usuario compartido, la atribución por persona es vía `ordenes_produccion.operario` (texto) ↔ `empleados.operario_nombre`; `computeParteHoras` (pura) suma productivos cerrados del mes, botón "⏱ Traer horas de planta" en el modal de liquidación sugiere bruto = horas × valor_hora (horas quedan en `item.detalle.horas_planta`), export "Parte de horas (CSV)" para la contadora. Provisión SAC: cuenta 214008 + `config.cta_provision_sac`; `confirmar_liquidacion` RE-EMITIDA con firma NUEVA (uuid, numeric, boolean — la de 2 params fue DROPeada; ediciones futuras parten de 066): `p_provisionar_sac` genera asiento aparte 'auto-prov-sac' (1/12 del BRUTO por centro, solo sueldo — las cargas del SAC van a gasto al devengarlo, simplificación pyme) y el tipo 'sac' consume la provisión (debe 214008 por min(saldo, gasto), resto a gasto con complemento exacto). `anular_liquidacion` re-emitida: anula también el asiento de provisión (los de PAGO no — plata movida). Alerta F.931 en computeAlertas (`alertaF931` pura: warn desde el día 2 del mes de vto, crit pasado el 9). Tests en `tests/sueldos.test.js`.
- **Sueldos Fase 1 (migración 065):** registro de nómina — la contadora liquida afuera (reforma laboral 2026: recibo digital vía ARCA, libro de sueldos eliminado por Dec. 407/2026 → el ERP NO liquida ni emite recibos). Tablas `empleados` (centro fabricacion/administracion → cuentas 421001/421002 vs 422003/422004; `operario_nombre` para linkear timers en Fase 2), `liquidaciones` + `liquidacion_items` (ARS; totales server-side; UNIQUE liquidación+empleado), todas con batería RLS completa. RPCs: `guardar_liquidacion` (borrador, valida neto = bruto + no_rem − aportes por ítem), `confirmar_liquidacion(p_liq_id, p_tipo_cambio)` (asiento de devengamiento vía crear_asiento con FECHA = fin de mes del período: gastos por centro al debe, 214006 neto + 214007 aportes+contribuciones+ART al haber; guarda TC para reportes USD), `anular_liquidacion` (confirmada → asiento anulado + vuelve a borrador con motivo al audit; borrador → delete). Config: 6 columnas `cta_sueldos_*`/`cta_cargas_*` con defaults del plan real + selects en Configuración → Imputación. Frontend: página `sueldos` (grupo contabilidad, módulo permisos propio), pre-cálculo editable con % (17 / 27,35 / 3 defaults 2026) vía `computeItemLiquidacion`/`computeTotalesLiquidacion` (puras, tests en `tests/sueldos.test.js`), pagos de netos y F.931+ART como asientos directos (origen_tipo `liquidacion-pago-netos/cargas` → badge PAGADO), cashflow proyectado suma netos (fecha de pago o fin de mes) y F.931 (día 9 del mes siguiente), solo fechas futuras y no pagadas. Pendiente Fase 2: parte de horas desde timers de Modo Planta, provisión SAC mensual, alerta F.931 en computeAlertas.
- **Entrega parcial (migración 064):** facturación parcial por DIVISIÓN de venta (no hay modelo de entregas/facturas parciales — decisión deliberada: cada parte es una venta común y todo el ciclo existente sirve sin cambios). RPC `dividir_venta(p_venta_id, p_cantidades [{venta_item_id,cantidad}], p_entregar)`: solo ventas `pendiente` sin CAE; la original queda con lo enviado (si `p_entregar` pasa a 'entregado' en la misma transacción → trigger stock 058 + fecha_entregado 026) y la hermana nace pendiente con el saldo, `venta_padre_id` → original, mismo `nro_pedido`, `nro_remito` con sufijo `-2/-3` (advisory lock `ventas_nro:`), `stock_descontado` HEREDADO (manual=true: nada que descontar; de presupuesto=false: cada parte descuenta al entregar). Totales: ítem prorrateado por cantidad con redondeo a 2 y saldo = complemento exacto (las partes siempre suman el original); cabecera prorrateada por totales de ítems. Espejo cliente `computeDivisionVenta` (pura, tests en `tests/entrega-parcial.test.js`) alimenta el preview del modal (botón "Parcial" en ventas pendientes con >1 unidad; badge SALDO en la hermana). La migración asserta que las columnas clonadas existan (el body plpgsql no se valida al crear).
- **Facturas UX (migración 062):** `fecha_contable` en facturas_recibidas (el asiento, libro IVA Compras y posición IVA van por la CONTABLE; la fecha de emisión es dato del comprobante — `registrar_factura_recibida` RE-EMITIDA en 062, ediciones futuras parten de ahí); RPC `anular_factura_recibida` (motivo obligatorio al audit_log vía UPDATE previo, asiento → estado 'anulado' — NUNCA se borra, patrón anular_venta —, factura DELETE con tax lines por cascade; bloqueada con NC/ND asociada; OC y recepciones intactas); frontend: Corregir = anular + reabrir modal precargado (`armarDraftCorreccion` pura + `_frAplicarDraft`), tilde "recibí todo" en factura con OC registra la recepción completa antes del RPC (`registrarRecepcionOC` extraída de saveRecepcion + `itemsPendientesOC` pura; `_frMatchInfo` simula el match con `{completo:true}`), `validarFechasFactura` pura (contable < emisión ⇒ warning no bloqueante), alta rápida +CUIT también en el modal de OC (`openProvRapidoModal(destino)`).
- **Match con moneda (migración 061):** re-emisión de `registrar_factura_recibida` (base 054 — ediciones futuras parten de 061): el 3-way match convierte el valor recibido de la moneda de la OC a la de la factura (TC factura > TC OC como fallback; monedas distintas sin TC ⇒ excepción NO bypasseable por override) y una OC sin recepciones (Σ cantidad_recibida = 0) da error claro "registrá la recepción primero" (override admin sigue disponible). Espejo cliente: `convertirRecibidoAFactura` pura (tests en compras-fiscal.test.js) + `_frMatchInfo` alimenta el preview del modal (badges SIN RECEPCIONES / FALTA TC / DISCREPANCIA).
- **Diferencial (migración 060):** tabla `planos` (UNIQUE empresa+codigo, file_data base64 on-demand como certificados; PLANTA puede SELECT — sin lockdown de lectura, sí de escritura — para el visor en planta.html `verPlano()`); vínculo con OPs por `codigo_plano` texto (celda clickeable en renderOP si el plano tiene archivo; página Producción → Planos, módulo permisos `planos`). Dashboard: `computeVentasPorMes` (12 meses, chart-fact-mensual en initDashCharts) y `computeCashflowProyectado` (FIFO cta cte + cheques vivos + facturas proveedor por vto — mismo criterio que las alertas de compras; NO descuenta pagos parciales a proveedores) — puras con tests en tests/diferencial.test.js.
- **Gotchas aprendidos:**
  - Las RPCs SIEMPRE devuelven `jsonb` (nunca `void`: el `db()` del frontend explota parseando la respuesta vacía).
  - Supabase otorga EXECUTE a `anon` por default privileges → toda función nueva lleva `REVOKE ... FROM anon` explícito.
  - Funciones con `SET search_path = public`.

## Arquitectura del frontend
- **Navegación:** sidebar de grupos + tabs por grupo. Página nueva = tocar TRES estructuras: `PAGE_TO_GROUP` (página→grupo), `GROUP_TABS` (tabs del grupo, soporta `sub:[]` para dropdowns), y `groupMods` dentro de `aplicarPermisos()`. El HTML de la página necesita `<div class="tabs"></div>` en su `.page-header` (showPage inyecta las tabs ahí). Sub-páginas además van en `PAGE_TO_PARENT`.
- **Datos:** `loadAll()` baja todo en un `Promise.all` con asignación por destructuring. Tabla nueva = agregar variable global, entrada AL FINAL del destructure y del array de GETs con `.catch(e=>_loadFail('Nombre',e))`, y fallback `||[]` en el catch de seguridad. Los errores de carga se muestran en el banner `#load-error-banner` (con botón Reintentar) — NO usar `.catch(()=>[])` silencioso.
- **Render:** `PAGE_RENDERS` mapea página → funciones de render. `renderAll()` marca todas las páginas sucias y renderiza SOLO la activa; `showPage()` renderiza una página sucia al entrar. Cada módulo: `renderModulo(filter='')` → stats en `#modulo-stats`, filtro por texto, tabla en `#modulo-tbody`. Página nueva → registrar sus renders en `PAGE_RENDERS`.
- **Estados:** `estadoSelect(tabla,id,actual,valores,badgeMap)` genera el select que PATCHea vía `cambiarEstado` genérico.
- **Save pattern:** validar → `setBusy(btnId,true)` → try { POST/PATCH o RPC + loadAll + closeModal + renderAll + notify } catch { notify err } finally setBusy false.
- **Escapado:** TODO dato de usuario interpolado en HTML pasa por `esc()`.
- **PDFs:** certificados se guardan base64 en la DB; `loadAll` NO baja `file_data` (se descarga on-demand en `openPDF`). Adjuntos ≤5MB. PDFs generados (presupuesto/remito/cert conformidad/NDP) usan jsPDF + doc.save().
- **Mobile (≤768px):** sidebar off-canvas con hamburguesa (`toggleMobileNav`), grids colapsan. Los overrides de estilos inline necesitan `!important` en la media query.
- **Digest:** `computeAlertas()` + panel "Para hoy" en el dashboard + badge en nav Métricas. Si un módulo nuevo tiene vencimientos/urgencias, sumarlos ahí.

## Módulos
Dashboard (KPIs + digest de alertas) · Materia prima (barras + reposición) · Productos terminados · Certificados MTC · Insumos · Herramientas · Órdenes de producción (tiempos por operación) · Compras (OCs, proveedores, recepciones, NDP) · Presupuestos (cotizador con costeo y margen, seguimiento, conversión) · Ventas (estilo Tango, remito PDF, cert de conformidad, OTD, botón Facturar→billing) · Clientes · Cta. corriente (aging FIFO) · Costos y rentabilidad · Trazabilidad (cert→barra→OP→PT→venta) · Contabilidad completa (plan de cuentas jerárquico, asientos, cobros y pagos, **cheques diferidos**, **sueldos** (nómina Fase 1: empleados + liquidaciones de la contadora + asiento de devengamiento + pagos netos/F.931), libro diario/mayor, balance comprobación, estado de resultados, balance general, ratios, bimonetario, ajuste por inflación RT 6, cierre/apertura, comparativos) · Configuración · Modo Planta (`planta.html` standalone: timers de producción desde el móvil vía QR por OP; usuario compartido `planta@vitalmetsa.com` con `usuarios.es_planta=true`, lockdown RLS RESTRICTIVE a ordenes_produccion/op_operaciones/op_time_entries + trigger solo-estado — migración 052. **Regla: toda tabla nueva con RLS necesita su policy `planta_lockdown`, las tres `contador_no_ins/upd/del` y el trigger `contador_guard`** (migración 053: Modo Contador — usuario `es_contador=true` en solo lectura total; los DO-loops de 052/053 son el molde)).

## Moneda
El core opera en **USD** (`fmtUSD()`). Excepciones: contabilidad bimonetaria y cheques (ARS o USD con tipo_cambio).

## Reglas de desarrollo
1. **Un cambio a la vez** — commit, deploy (push a main) y verificar entre cambios.
2. **SQL en Supabase ANTES del código** — pasar la migración para el SQL Editor, esperar el "listo", verificar, recién ahí pushear el frontend.
3. **empresa_id obligatorio** en todo POST (`currentEmpresa.id`).
4. **No usar parseFloat en campos de texto** (ej: `horas_total` es text).
5. **Columnas thead vs tbody deben coincidir.**
6. Página nueva → `PAGE_TO_GROUP` + `GROUP_TABS` + `groupMods` en `aplicarPermisos` + `<div class="tabs">`.
7. Tabla nueva → global + `loadAll` (al final, con `_loadFail`) + `renderAll`.
8. Mutaciones críticas (ventas, asientos, stock) → usar las RPCs atómicas, no POST/PATCH directo.
9. Validar antes de commitear: extraer el `<script>` grande y `node --check`, y correr `node --test tests/calculos.test.js`. Si se toca un cálculo de plata (cta cte, costeo, reposición), agregar/ajustar su test.
10. Texto de UI en español argentino (voseo).

## Estructura del archivo
```
index.html (~8.050 líneas)
├── <head> — fonts + libs CDN (líneas 10-14)
├── <style> (15-249) — CSS completo (mobile al final, ~216+)
├── <body>
│   ├── #loading-screen (~256)
│   ├── #auth-screen (~266) — login/register/join
│   ├── mobile-menu-btn + backdrop + #load-error-banner (~351)
│   ├── #app-screen (~356) — sidebar + pages (page-dash ... page-config)
│   ├── Modales
│   └── <script> (1841-fin) — toda la lógica
```

## Pendientes (deuda técnica de la auditoría 2026-06)
- [x] Errores de carga visibles (banner + Reintentar)
- [x] Headers CSP (netlify.toml)
- [x] Tests de cálculos de plata (`tests/calculos.test.js` — cta cte FIFO/aging, costeo PT, reposición; harness vm en `tests/_harness.js`)
- [x] Pase de `esc()` sobre renders viejos (104 interpolaciones; los contextos sin HTML — notify/confirm/textContent/DB/jsPDF — no se escapan)
- [ ] Partir index.html en módulos (script files clásicos) — solo cuando haya dolor real
- [x] Render solo de la página activa (PAGE_RENDERS + dirty-set en showPage)
- [ ] PDFs a Supabase Storage (hoy base64 en la DB)
- [ ] Decisión: permisos por módulo son solo visuales (REST accesible a todo usuario autenticado)
- [ ] Padrón AFIP (bloqueado por cert X.509; cuando esté: GET /padron/{cuit} en billing + botón "Buscar en AFIP")

### Logo
SVG embebido en base64 dentro del HTML (loading screen, auth, sidebar): 4 paralelogramos azules/grises + texto "VITALSTOCK". Favicon también SVG embebido.
